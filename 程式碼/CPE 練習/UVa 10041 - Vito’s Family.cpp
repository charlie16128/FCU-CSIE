#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc;
    cin >> tc;

    while(tc--){
        int r;
        vector<int> s(r);

        cin >> r;
        for(auto &x : s){
            cin >> x;
        }

        sort(s.begin(), s.end());

        int mid = s[r / 2];

        
    }

    return 0;
}